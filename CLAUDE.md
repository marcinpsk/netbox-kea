# CLAUDE.md

Repo guidance for AI coding agents lives in **[AGENTS.md](AGENTS.md)** — the single
source of truth for architecture, build/test/lint, testing philosophy, security
rules, and conventions. Read it first.

Global user preferences (from `~/.claude/CLAUDE.md`) still apply on top of it.

## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five default triage labels when classifying issues. See `docs/agents/triage-labels.md`.

### Domain docs

Use the single-context domain documentation layout. See `docs/agents/domain.md`.
